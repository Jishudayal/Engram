# MemoryEval

Benchmark suite for measuring AI memory system reliability.

```bash
pip install memoryeval
memoryeval run --backend qdrant --config config.yaml
```

Part of the [Engram](https://github.com/Jishudayal/Engram) project.
